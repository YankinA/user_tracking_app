{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc -watch",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "typescript": "^4.0.0"
  }
}
